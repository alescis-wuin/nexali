# Protocols

This directory will contain versioned protocol specifications and compatibility notes for:

- HTTP/API contracts.
- Synchronization protocol and cursors.
- Cryptographic object/envelope formats.
- Key distribution and rotation.

Do not document a cryptographic format as final before its required security spike and interop test vectors are complete.
